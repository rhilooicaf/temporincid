// Define the object with an array property
let f = {
    ring: []
};

// Define the item to be added
let end = "someValue";

// Push the item into the array
f.ring.push(end);

// Print the updated array
console.log(f.ring); // Output: ["someValue"]
